@layout.authLayout({title:"login"})

@slot('content')
        <form method="post" action="{{ route('auth.login_step2') }}" id="auth_login"
              class="bg-[#16181c] rounded-xl w-full max-w-md p-6 shadow-lg shadow-gray-900/50">

          <div class="flex justify-center mb-4">
            <img
              src="https://upload.wikimedia.org/wikipedia/commons/2/22/X_logo_2023.svg"
              alt="Logo X"
              class="h-6"
            />
          </div>

          {{ csrfField() }}

          <h2 class="text-2xl font-bold text-center mb-6">
            Connectez-vous à X
          </h2>

          <!-- Connexion avec Google -->
          <div class="space-y-3">
            <button
              type="button"
              class="w-full flex items-center justify-center gap-2 bg-white text-black py-2 rounded-full hover:bg-gray-200 transition"
            >
              <img
                src="https://upload.wikimedia.org/wikipedia/commons/5/53/Google_%22G%22_Logo.svg"
                alt="Google"
                class="w-5 h-5"
              />
              Se connecter avec Gmail
            </button>
          </div>

          <!-- Séparateur -->
          <div class="flex items-center my-4">
            <div class="flex-grow h-px bg-gray-600"></div>
            <span class="px-2 text-gray-400 text-sm">ou</span>
            <div class="flex-grow h-px bg-gray-600"></div>
          </div>

          @!component('components/input', {
            type: 'email',
            name: 'email',
            title: 'Adresse email'
          })

          <!-- Bouton Suivant -->
          <button
            type="submit"
            class="w-full bg-white text-black py-3 rounded-full font-semibold hover:bg-gray-200 transition mb-3"
          >
            Suivant
          </button>

          <!-- Mot de passe oublié -->
          <button
            type="button"
            class="w-full border border-gray-600 text-white py-2 rounded-full hover:bg-gray-800 transition mb-6"
          >
            Mot de passe oublié ?
          </button>

          <!-- Lien d’inscription -->
          <p class="text-sm text-center text-gray-400">
            Vous n’avez pas de compte ?
            <a href="{{ route('register') }}" class="text-blue-500 hover:underline">
              Inscrivez-vous
            </a>
          </p>

        </form>
          
@endslot
@end
